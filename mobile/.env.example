# Optional override. If unset, the app uses your Expo host LAN IP :8000
# so a physical phone can reach the FastAPI backend.
# Android emulator: http://10.0.2.2:8000
# iOS simulator:    http://127.0.0.1:8000
# Physical device:  http://YOUR_LAN_IP:8000
EXPO_PUBLIC_API_URL=
